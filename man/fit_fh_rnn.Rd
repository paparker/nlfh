% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-rnn.R
\name{fit_fh_rnn}
\alias{fit_fh_rnn}
\title{Fit a Random-Weight Neural Network Fay-Herriot Model}
\usage{
fit_fh_rnn(
  y = NULL,
  x = NULL,
  sampling_variance = NULL,
  formula = NULL,
  data = NULL,
  X = NULL,
  n_hidden = 200,
  prior_beta_variance = NULL,
  prior_shape = 0.1,
  prior_rate = 0.1,
  n_iter = 1000,
  burn_in = 500,
  scale = TRUE,
  progress = TRUE
)
}
\arguments{
\item{y}{Numeric vector of area-level direct estimates for the matrix
interface. If the first argument is a formula, it is treated as \code{formula}.}

\item{x, X}{Numeric matrix or data frame of area-level covariates for the
matrix interface. Rows must correspond to entries of \code{y}. Include an
intercept column if one is desired.}

\item{sampling_variance}{Numeric vector of known sampling variances for \code{y}.
With the formula interface, this may also be an unquoted column name from
\code{data} or a length-one character string naming a column in \code{data}.}

\item{formula}{Optional model formula such as \code{y ~ x1 + x2}. For nonlinear
models, the formula specifies the predictors available to the model; it
does not imply an additive linear mean structure.}

\item{data}{Optional data frame containing variables used by \code{formula} and,
optionally, \code{sampling_variance}.}

\item{n_hidden}{Positive integer number of hidden nodes in the random-weight
neural network.}

\item{prior_beta_variance}{Optional positive scalar prior variance for the
output-layer coefficients. When \code{NULL}, the coefficient variance is sampled
with the original inverse-gamma update.}

\item{prior_shape}{Non-negative scalar shape parameter for the inverse-gamma
prior on the random-effect variance.}

\item{prior_rate}{Non-negative scalar rate parameter for the inverse-gamma
prior on the random-effect variance.}

\item{n_iter}{Positive integer number of MCMC iterations.}

\item{burn_in}{Positive integer number of initial MCMC iterations to discard.}

\item{scale}{Logical; if \code{TRUE}, center and scale non-intercept covariates
before fitting. Intercept columns named \code{(Intercept)}, \code{Intercept}, or
\code{intercept} are not scaled.}

\item{progress}{Logical; if \code{TRUE}, display a progress bar.}
}
\value{
An object of class \code{nlfh_rnn_fit} and \code{nlfh_fit}, a list with
posterior draws for \code{predictions}, \code{random_effect_variance},
\code{coefficient_variance}, hidden-layer \code{coefficients}, \code{mean}, the scalar
\code{dic}, and MCMC metadata.
}
\description{
Fits a Bayesian Fay-Herriot model whose mean function is represented by a
fixed random hidden layer with logistic activation and sampled output-layer
coefficients.
}
\details{
Formula inputs are parsed with \code{\link[stats:model.frame]{stats::model.frame()}} and
\code{\link[stats:model.matrix]{stats::model.matrix()}}. Factors are expanded using R's standard contrast and
dummy-variable rules. An intercept is included when the formula includes one,
which is the default; matrix inputs are used as supplied. For this nonlinear
method, the formula specifies the available predictors and does not impose an
additive linear mean structure. The model estimates an unknown function
\code{f(X)}.

The response and sampling variances are standardized internally before
fitting the RNN. Posterior predictions, mean function draws, coefficients,
random-effect variances, and DIC are transformed back to the original
response scale before being returned.
}
\examples{
data(acs_dat)
acs_small <- as.data.frame(acs_dat[1:500, ])
fit <- fit_fh_rnn(
  MedInc ~ .,
  sampling_variance = MedIncSE^2,
  data = acs_small,
  n_iter = 500,
  burn_in = 250,
  progress = FALSE
)
summary(fit)
}
\references{
Parker, P. A. (2024). Nonlinear Fay-Herriot Models for Small Area Estimation
Using Random Weight Neural Networks. \emph{Journal of Official Statistics},
40(2), 317-332. \doi{10.1177/0282423X241244671}
}
