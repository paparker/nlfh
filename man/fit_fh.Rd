% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_fh}
\alias{fit_fh}
\title{Fit a Fay-Herriot Model}
\usage{
fit_fh(
  y = NULL,
  x = NULL,
  sampling_variance = NULL,
  method = c("linear", "rnn", "bart"),
  control = list(),
  data = NULL,
  formula = NULL,
  X = NULL
)
}
\arguments{
\item{y}{Numeric vector of area-level direct estimates for the matrix
interface.}

\item{x, X}{Numeric matrix or data frame of area-level covariates for the
matrix interface. Rows must correspond to entries of \code{y}. Include an
intercept column if one is desired.}

\item{sampling_variance}{Numeric vector of known sampling variances for \code{y}.
With the formula interface, this may also be an unquoted column name from
\code{data} or a length-one character string naming a column in \code{data}.}

\item{method}{Character string selecting the model. Options are \code{"linear"}
for the linear Fay-Herriot model, \code{"rnn"} for the random-weight neural
network Fay-Herriot model, and \code{"bart"} for the BART Fay-Herriot model.}

\item{control}{Named list of control parameters. Common controls are
\code{n_iter}, \code{burn_in}, and \code{progress}. Linear-specific control:
\code{prior_beta_variance}. RNN-specific control: \code{n_hidden}. BART-specific
controls: \code{prior_shape}, \code{prior_rate}, \code{n_bart_samples}, and \code{n_trees}.}

\item{data}{Optional data frame containing variables used by \code{formula} and,
optionally, \code{sampling_variance}.}

\item{formula}{Optional model formula such as \code{y ~ x1 + x2}. If the first
argument is a formula, it is treated as \code{formula}. For nonlinear methods,
the formula specifies the predictors available to the model; it does not
imply an additive linear mean structure. Nonlinearities and interactions
may be learned implicitly by the selected model.}
}
\value{
An object inheriting from \code{nlfh_fit}. The first class identifies the
fitted method: \code{nlfh_linear_fit}, \code{nlfh_rnn_fit}, or \code{nlfh_bart_fit}.
}
\description{
Primary user-facing model fitting function for linear and nonlinear
Fay-Herriot models. Use \code{method} to choose the mean-function model and
\code{control} to pass method-specific tuning parameters.
}
\details{
Formula inputs are parsed with \code{\link[stats:model.frame]{stats::model.frame()}} and
\code{\link[stats:model.matrix]{stats::model.matrix()}}. Factors are expanded using R's standard contrast and
dummy-variable rules. An intercept is included when the formula includes one,
which is the default for formulas such as \code{y ~ x1 + x2}; use \verb{0 +} or \code{- 1}
in the formula to omit it. Matrix inputs are used as supplied, so include an
intercept column manually if one is desired.

For \code{method = "rnn"} and \code{method = "bart"}, the formula identifies the
predictors available to the nonlinear mean function. It does not impose an
additive linear model. These methods estimate an unknown function \code{f(X)}, and
nonlinearities or interactions may be learned implicitly by the model.
\code{sampling_variance} is always supplied separately from the formula. When
\code{sampling_variance} names a column in \code{data}, that column is excluded from
\code{y ~ .} expansion.
}
\examples{
data(acs_dat)
acs_small <- as.data.frame(acs_dat[1:500, ])
example_control <- list(n_iter = 500, burn_in = 250, progress = FALSE)

fit_linear <- fit_fh(
  MedInc ~ SNAPRate + PovRate + White + Black + Hispanic + Asian,
  sampling_variance = MedIncSE^2,
  data = acs_small,
  method = "linear",
  control = example_control
)

X <- model.matrix(
  MedInc ~ SNAPRate + PovRate + White + Black + Hispanic + Asian,
  data = acs_small
)
fit_matrix <- fit_fh(
  y = acs_small$MedInc,
  X = X,
  sampling_variance = acs_small$MedIncSE^2,
  method = "linear",
  control = example_control
)

fit_rnn <- fit_fh(
  MedInc ~ .,
  sampling_variance = MedIncSE^2,
  data = acs_small,
  method = "rnn",
  control = example_control
)

fit_bart <- fit_fh(
  MedInc ~ SNAPRate + PovRate + White,
  sampling_variance = MedIncSE^2,
  data = acs_small,
  method = "bart",
  control = example_control
)
}
