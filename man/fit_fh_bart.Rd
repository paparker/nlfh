% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-bart.R
\name{fit_fh_bart}
\alias{fit_fh_bart}
\alias{BARTFH}
\title{Fit a BART Fay-Herriot Model}
\usage{
fit_fh_bart(
  y = NULL,
  x = NULL,
  sampling_variance = NULL,
  formula = NULL,
  data = NULL,
  X = NULL,
  prior_shape = 0.01,
  prior_rate = 0.01,
  n_iter = 1000,
  burn_in = 500,
  n_bart_samples = 10,
  n_trees = 50,
  progress = TRUE
)

BARTFH(
  y,
  x,
  D,
  a = 0.01,
  b = 0.01,
  n.iter = 1000,
  n.burn = 500,
  n.bart.samples = 10,
  n.tree = 50
)
}
\arguments{
\item{y}{Numeric vector of area-level direct estimates for the matrix
interface. If the first argument is a formula, it is treated as \code{formula}.}

\item{x, X}{Numeric matrix or data frame of area-level covariates for the
matrix interface. Rows must correspond to entries of \code{y}.}

\item{sampling_variance}{Numeric vector of known sampling variances for \code{y}.
With the formula interface, this may also be an unquoted column name from
\code{data} or a length-one character string naming a column in \code{data}.}

\item{formula}{Optional model formula such as \code{y ~ x1 + x2}. For nonlinear
models, the formula specifies the predictors available to the model; it
does not imply an additive linear mean structure.}

\item{data}{Optional data frame containing variables used by \code{formula} and,
optionally, \code{sampling_variance}.}

\item{prior_shape}{Non-negative scalar shape parameter for the inverse-gamma
prior on the random-effect variance.}

\item{prior_rate}{Non-negative scalar rate parameter for the inverse-gamma
prior on the random-effect variance.}

\item{n_iter}{Positive integer number of MCMC iterations.}

\item{burn_in}{Positive integer number of initial MCMC iterations to discard.}

\item{n_bart_samples}{Positive integer number of BART samples to draw per
outer MCMC iteration.}

\item{n_trees}{Positive integer number of trees used by \code{dbarts}.}

\item{progress}{Logical; if \code{TRUE}, display a progress bar.}

\item{D}{Legacy argument name for \code{sampling_variance}.}

\item{a, b}{Legacy argument names for \code{prior_shape} and \code{prior_rate}.}

\item{n.iter, n.burn}{Legacy argument names for \code{n_iter} and \code{burn_in}.}

\item{n.bart.samples}{Legacy argument name for \code{n_bart_samples}.}

\item{n.tree}{Legacy argument name for \code{n_trees}.}
}
\value{
An object of class \code{nlfh_bart_fit} and \code{nlfh_fit}, a list with
posterior draws for \code{predictions}, the BART mean function \code{mean}, random effects
\code{random_effects}, random-effect variance \code{random_effect_variance},
\code{variable_importance}, the scalar \code{dic}, and MCMC metadata.
}
\description{
Fits a Bayesian Fay-Herriot model whose mean function is represented with
Bayesian additive regression trees via the \code{dbarts} package. The sampling
model, priors, and MCMC updates match the original \code{BARTFH()} implementation.
}
\details{
Formula inputs are parsed with \code{\link[stats:model.frame]{stats::model.frame()}} and
\code{\link[stats:model.matrix]{stats::model.matrix()}}. Factors are expanded using R's standard contrast and
dummy-variable rules. An intercept is included when the formula includes one,
which is the default; matrix inputs are used as supplied. For this nonlinear
method, the formula specifies the available predictors and does not impose an
additive linear mean structure. The BART mean component estimates an unknown
function \code{f(X)}.
}
\examples{
data(acs_dat)
acs_small <- as.data.frame(acs_dat[1:500, ])
fit <- fit_fh_bart(
  MedInc ~ SNAPRate + PovRate + White,
  sampling_variance = MedIncSE^2,
  data = acs_small,
  n_iter = 500,
  burn_in = 250,
  progress = FALSE
)
summary(fit)
}
\references{
Parker, P. A. and Eideh, A. (2026). BART-FH: Flexible Nonlinear Modeling for
Small Area Estimation. \emph{Journal of Survey Statistics and Methodology},
00, 1-18. \doi{10.1093/jssam/smaf050}
}
